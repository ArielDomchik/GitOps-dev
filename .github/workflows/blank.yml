name: Replace dev with prod

on:
  push:
    branches:
      - main

jobs:
  replace:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout dev
        uses: actions/checkout@v2
        with:
          repository: ArielDomchik/GitOps-dev
          token: ${{ secrets.API_TOKEN_GITHUB }}
          path: ./repositories/GitOps-dev
      
      - name: Clone prod
        uses: actions/checkout@v2
        with:
          repository: ArielDomchik/GitOps-prod
          token: ${{ secrets.API_TOKEN_GITHUB }}
          path: ./repositories/GitOps-prod
      
      - name: Clone Personal-space
        uses: actions/checkout@v2
        with:
          repository: ArielDomchik/Personal-space
          token: ${{ secrets.API_TOKEN_GITHUB }}
          path: ./repositories/Personal-space

      - name: Replace dev with prod
        run: |
          cd ./repositories
          rsync -r --exclude='values.yaml' --exclude='README.md' ./GitOps-dev/ ./GitOps-prod/
          cp ./GitOps-dev/values.yaml ./Personal-space/values.yaml
        working-directory: ./repositories
        
      - name: Commit changes
        run: |
          cd ./repositories/GitOps-prod/
          git config --global user.name "ArielDomchik"
          git config --global user.email "ariyonzor@gmail.com"
          git add .
          git commit -m "Replace dev with prod"
          git push
          
          cd ../Personal-space/
          git add values.yaml
          git commit -m "Update values.yaml from GitOps-dev"
          git push
        working-directory: ./repositories
