apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: hello-kubernetes-ingress
  namespace: default
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: "a36f3827639e148baa898f198bdea073-469817302.us-east-1.elb.amazonaws.com"
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: weather-app
            port:
              number: 80
  - host: "a36f3827639e148baa898f198bdea073-469817302.us-east-1.elb.amazonaws.com"
    http:
      paths:
      - pathType: Prefix
        path: "/solitaire"
        backend:
          service:
            name: solitaire
            port:
              number: 80
